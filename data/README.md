# Data

Raw datasets are not committed to this repository.

Use this directory for dataset notes, checksums, or small metadata files. Keep
downloaded datasets in `data/raw/`, which is ignored by Git.

